name: Apply mobile responsive tuning

on:
  push:
    branches:
      - agent/mobile-responsive-tuning

permissions:
  contents: write

jobs:
  patch:
    if: github.event.head_commit.message != 'Apply mobile responsive tuning'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout
        uses: actions/checkout@fbc6f3992d24b796d5a048ff273f7fcc4a7b6c09
        with:
          ref: agent/mobile-responsive-tuning
      - name: Patch mobile styles
        run: |
          python - <<'PY'
          from pathlib import Path

          path = Path('src/pages/index.astro')
          text = path.read_text()
          old = '''  @media (max-width: 760px) {
            .portal-hero__footer,
            .portal-intro__grid,
            .portal-next__grid {
              grid-template-columns: 1fr;
            }

            .portal-hero__content {
              min-height: calc(100svh - 5rem);
              padding-block: 1.5rem 2.4rem;
            }

            .portal-hero h1 {
              font-size: clamp(4.2rem, 20vw, 7rem);
            }

            .portal-hero__footer {
              align-items: start;
              gap: 1.2rem;
            }

            .portal-services {
              grid-template-columns: 1fr;
            }

            .portal-card,
            .portal-card--wide {
              grid-column: auto;
              min-height: 78svh;
            }

            .portal-card__content {
              gap: 1rem;
            }

            .portal-card__content h3,
            .portal-card--wide .portal-card__content h3 {
              font-size: clamp(2rem, 9vw, 3rem);
            }

            .portal-next__grid {
              gap: 2rem;
            }
          }
          '''
          new = '''  @media (max-width: 760px) {
            .portal-hero__footer,
            .portal-intro__grid,
            .portal-next__grid {
              grid-template-columns: 1fr;
            }

            .portal-hero,
            .portal-hero__content {
              min-height: 72svh;
            }

            .portal-hero__content {
              padding-block: 1.25rem 1.8rem;
            }

            .portal-hero h1 {
              font-size: clamp(3.4rem, 16vw, 5.4rem);
            }

            .portal-hero__footer {
              align-items: start;
              gap: 0.9rem;
              margin-top: 1.6rem;
            }

            .portal-hero__footer p {
              font-size: 0.9rem;
              line-height: 1.65;
            }

            .portal-intro {
              padding: 4rem 0;
            }

            .portal-intro h2,
            .portal-next h2 {
              font-size: clamp(2.6rem, 12vw, 4rem);
            }

            .portal-services {
              grid-template-columns: 1fr;
            }

            .portal-card,
            .portal-card--wide {
              grid-column: auto;
              min-height: 54svh;
              padding: 1.25rem;
            }

            .portal-card--wide {
              min-height: 60svh;
            }

            .portal-card__content {
              gap: 0.8rem;
            }

            .portal-card__content h3,
            .portal-card--wide .portal-card__content h3 {
              font-size: clamp(1.55rem, 7vw, 2.25rem);
              line-height: 1.16;
            }

            .portal-card__arrow {
              width: 2.8rem;
              font-size: 1.1rem;
            }

            .portal-next {
              padding: 4.5rem 0;
            }

            .portal-next__grid {
              gap: 2rem;
            }
          }
          '''
          old = old.replace('          ', '')
          new = new.replace('          ', '')
          if old not in text:
              raise SystemExit('Expected mobile style block not found')
          path.write_text(text.replace(old, new))
          PY
      - name: Commit tuning
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add src/pages/index.astro
          git commit -m "Apply mobile responsive tuning"
          git push origin HEAD:agent/mobile-responsive-tuning
